from simulate_par import find_links, find_clusters
import numpy as np

N=3
#a = np.random.choice([1, -1], size = [N, N])
a = np.array([
		[-1, -1,  1], 
		[ 1, -1,  1], 
		[ 1,  1, -1]])

links = find_links(N, a, 1000)
clusters, largest_label, list_of_labels = find_clusters(N, a, links)
print("-----------------")
print(a)

